# ===========================================
# MyClo Platform - Unified Environment Variables
# ===========================================
# 이 파일은 프로젝트의 모든 환경변수 설정을 통합 관리합니다.
# 개발 시 이 파일을 복사하여 .env 파일을 만들고 실제 값으로 채워주세요.
# .env 파일은 보안 상 Git에 커밋되지 않습니다 (.gitignore 참조).

# --- Backend API Configuration ---
# FastAPI 서버 설정
PORT=8000
HOST=0.0.0.0
SECRET_KEY=your-fallback-secret-key-change-in-prod-use-strong-secret-here
API_VERSION=v1

# --- Database Configuration (Supabase) ---
# Supabase PostgreSQL 데이터베이스 연결 정보
# Project Settings → Database → Connection string 에서 확인 가능
# 형식: postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# --- AI Services ---
# Google Gemini API 설정
# API 키 발급: https://makersuite.google.com/app/apikey
# Gemini 1.5 Flash 추천 (비용 효율적이고 빠름)
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-1.5-flash
GEMINI_VISION_MODEL=gemini-1.5-flash

# --- Weather API ---
# 기상청 동네예보 API 설정
# API 키 발급: https://data.kma.go.kr/dataPortal/list/selectDataApiList.do
# 서비스키 발급 후 URL 인코딩된 값과 디코딩된 값 모두 저장 가능
KMA_API_KEY=your_kma_api_key_here

# --- Supabase Configuration ---
# Supabase 프로젝트 연결 정보
# Project Settings → API 에서 확인 가능
SUPABASE_URL=https://[YOUR-PROJECT-REF].supabase.co
SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_KEY=your_service_role_key_here
SUPABASE_STORAGE_BUCKET=wardrobe-images

# --- File Upload & Storage Configuration ---
# 파일 업로드 관련 설정
# Supabase Storage 버킷 설정과 일치해야 함
MAX_FILE_SIZE=10485760  # 10MB (10 * 1024 * 1024 bytes)
ALLOWED_EXTENSIONS=jpg,jpeg,png,gif,webp
OUTPUT_DIR=extracted_attributes  # 로컬 임시 저장 디렉토리 (백엔드)

# --- Frontend Configuration ---
# Vite 빌드 시스템 환경변수 (VITE_ 접두사 필수)
# 프론트엔드 애플리케이션 설정
VITE_API_URL=http://localhost:8000
VITE_API_BASE_URL=http://localhost:8000
VITE_API_VERSION=v1
VITE_APP_NAME=MyClo
VITE_APP_VERSION=1.0.0
VITE_APP_DESCRIPTION=AI 기반 옷장 관리 및 코디 추천 서비스

# Supabase 프론트엔드 연결 정보
VITE_SUPABASE_URL=https://[YOUR-PROJECT-REF].supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# --- Development & Production Environment ---
# 개발 환경 설정
NODE_ENV=development
DEBUG=true
LOG_LEVEL=info

# --- CORS Configuration ---
# 백엔드 CORS 설정 (개발 환경용)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173

# --- Optional: Additional Services ---
# 향후 확장을 위한 설정 (현재는 주석 처리)
# REDIS_URL=redis://localhost:6379
# STRIPE_SECRET_KEY=sk_test_...
# GOOGLE_ANALYTICS_ID=GA-XXXXXXXXX
# SENTRY_DSN=https://[your-sentry-dsn]

# --- Virtual Wardrobe Image Processing ---
# 옷 이미지 처리를 위한 선택적 서비스 설정
# REMOVE_BG_API_KEY=your_remove_bg_api_key_here  # https://www.remove.bg/api